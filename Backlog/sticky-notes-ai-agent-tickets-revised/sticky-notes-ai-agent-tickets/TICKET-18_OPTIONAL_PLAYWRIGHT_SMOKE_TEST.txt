TICKET ID: SN-18
TITLE: OPTIONAL - Add one Chromium Playwright end-to-end smoke test
PRIORITY: OPTIONAL / FIRST TO CUT
DEPENDS ON: SN-17 and all must-ship work stable
PRIMARY FILES:
- Minimal Playwright configuration
- One smoke spec

EXECUTION GATE
DO NOT START THIS TICKET unless:
- All must-ship tickets are done.
- npm run check passes.
- Manual verification is complete.
- No P0/P1 defect remains.
- There is sufficient time without risking submission quality.

GOAL
Add exactly one Chromium smoke test that validates the main happy path without creating generalized browser-test infrastructure.

SCENARIO
1. Open the app.
2. Enter create mode.
3. Drag to create a note.
4. Type text.
5. Move the note.
6. Resize the note.
7. Reload.
8. Confirm text/persistence.
9. Drag the note so the release pointer is over trash.
10. Confirm deletion.

CONSTRAINTS
- Chromium only.
- One spec.
- No page object model.
- No helper framework beyond a few local functions if necessary.
- No cross-browser matrix.
- No visual snapshots.
- No CI architecture unless already trivial.
- Do not change production behavior to make selectors convenient; add stable data-testid attributes only where semantic selectors are not reliable.
- Keep test deterministic and independent of existing localStorage by clearing/seeding it explicitly.

PACKAGE INTEGRATION
If completed:
- Add a clearly named optional e2e script.
- Do not make npm run check depend on large browser installation unless the submission environment supports it and this does not harm evaluator setup.
- Document how to run the smoke test separately.
- Update README tested-browser facts truthfully.

ACCEPTANCE CRITERIA
- The single scenario passes reliably.
- No unnecessary test infrastructure is introduced.
- Must-ship quality remains unchanged.
- Ticket can be omitted entirely with no loss of required implementation.

DONE WHEN
A reviewer can run one concise Chromium happy-path test, but only after the required assessment is already complete.
