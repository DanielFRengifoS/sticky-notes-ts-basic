import './Board.css';

export function Board() {
  return (
    <main className="board" aria-label="Sticky notes workspace">
      <p className="board__placeholder">Workspace scaffold ready.</p>
    </main>
  );
}
